services:
  docs-seeker:
    build: .
    container_name: docs-seeker
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    restart: unless-stopped
    command: uvicorn docs_seeker.app:app --host 0.0.0.0 --port 8001 --reload
